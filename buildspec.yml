version: 0.2
phases:
  install: 
    commands:
      - npm i npm@latest -g
  pre_build:
    commands:
     - npm install --fix
     - npm run manifest
     - npm test
  build:
    commands:
      - npm run build
artifacts:
  files:
    -  '**/*'
  base-directory: 'dist'