version: 0.2

phases:
  install: 
    commands:
      - npm i npm@latest -g
  pre_build:
    commands:
     - npm install --fix
     - npm test
  build:
    commands:
      - npm run build
      - cd dist
  post_build:
    commands:
      - aws s3 sync . $S3_BUCKET